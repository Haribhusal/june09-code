const courses = [
    {
        title: "Web Development",
        modules: ["HTML", "CSS", "JavaScript"]
    },
    {
        title: "Backend Development",
        modules: ["Node.js", "Express", "MongoDB"]
    }
];

// const moduleLists = courses.map(course => course.modules);
// console.log(moduleLists);

// const allModules = courses.flatMap(course => course.modules);
// console.log(allModules);